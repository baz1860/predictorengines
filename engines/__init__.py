"""Engine implementations (refactor Phase 3).

Distinct from app/engines/, which holds the thin *adapters* that the app/registry use.
This package holds the actual model/engine code. The other sports (golf, cfb,
club_soccer) will move under here in a later step.
"""
